---
phase: 15-admin-church-outreach-research-management
plan: 12
type: execute
wave: 6
depends_on: [15-06, 15-09, 15-10, 15-11]
files_modified:
  - server.js
  - src/app/(admin)/admin/church-outreach/page.tsx
autonomous: false
requirements:
  - CO-14

must_haves:
  truths:
    - "Drip scheduler cron is initialized in server.js alongside existing schedulers"
    - "All church outreach tabs load without errors"
    - "Build completes without TypeScript errors"
    - "Landing page renders at /sample-request"
  artifacts:
    - path: "server.js"
      provides: "Drip scheduler initialization call"
      contains: "initDripScheduler"
  key_links:
    - from: "server.js"
      to: "src/lib/church-outreach/drip-scheduler.ts"
      via: "dynamic import and init call"
      pattern: "initDripScheduler"
---

<objective>
Wire the drip scheduler into server.js, finalize all tab component lazy loading, and verify the complete phase builds and runs.

Purpose: Integration ensures all components work together end-to-end.
Output: Updated server.js, finalized page, build verification
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-admin-church-outreach-research-management/15-06-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-09-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-10-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-11-SUMMARY.md
@server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire drip scheduler into server.js and finalize page wiring</name>
  <files>
    server.js
    src/app/(admin)/admin/church-outreach/page.tsx
  </files>
  <action>
**server.js:**
Find the existing scheduler initialization section (where initEmailScheduler or similar cron setup happens). Add drip scheduler initialization using the same pattern:

```javascript
// In the server setup section, after existing scheduler init:
import('./src/lib/church-outreach/drip-scheduler.ts')
  .then(({ initDripScheduler }) => initDripScheduler())
  .catch(err => console.error('[Drip Scheduler] Init failed:', err));
```

Note: server.js is a .js file that can't import TS directly. Use dynamic import which goes through the Next.js module resolution. Follow the exact same pattern used for other scheduler initializations in server.js.

If server.js uses a setTimeout pattern for scheduler init (like the existing 5s fallback), follow that pattern:
```javascript
setTimeout(() => {
  import('./src/lib/church-outreach/drip-scheduler.ts')
    .then(m => m.initDripScheduler())
    .catch(err => console.error('[Drip Scheduler] Init failed:', err));
}, 5000);
```

**src/app/(admin)/admin/church-outreach/page.tsx:**
Verify that all 6 tabs are wired to their actual components (not placeholders):
- Dashboard → OutreachDashboard
- Discovery → DiscoverySearch
- Pipeline → KanbanBoard
- Campaigns → CampaignManager
- Sequences → SequenceManager
- Reports → ReportsView

All loaded via React.lazy with Suspense fallback.
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Drip scheduler wired into server.js; all 6 tabs load real components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete church outreach CRM</name>
  <files>src/app/(admin)/admin/church-outreach/page.tsx, src/app/sample-request/page.tsx</files>
  <action>
Verify the complete church outreach CRM system: database (11 tables), discovery pipeline (Google Places + scraping + AI), kanban pipeline board, email campaigns with templates and tracking, drip sequences, public sample request landing page, sample/conversion tracking, and reporting dashboard.

Steps:
1. Start the dev server: `npm run dev`
2. Navigate to /admin/church-outreach
3. Verify the Dashboard tab shows (it will show zeros for a fresh database)
4. Click the Discovery tab -- verify search form appears
5. Click the Pipeline tab -- verify kanban board with 7 columns appears
6. Click the Campaigns tab -- verify campaign list (empty) and create form
7. Click the Sequences tab -- verify sequence list (empty) and create button
8. Click the Reports tab -- verify reports dashboard loads
9. Navigate to /sample-request -- verify the public landing page renders
10. Submit the sample request form -- verify thank-you page redirect
11. Run `npx next build` to verify production build succeeds
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 admin tabs render, landing page works, build succeeds, drip scheduler initializes</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All admin tabs render correctly
- Public landing page renders and form submits
- Drip scheduler initializes on server start (check console logs)
</verification>

<success_criteria>
- Full build succeeds with zero TypeScript errors
- All 6 admin tabs functional
- Landing page accessible without auth
- Drip scheduler cron active
- End-to-end flow: discover → import → pipeline → campaign → sample → conversion
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-church-outreach-research-management/15-12-SUMMARY.md`
</output>
