---
phase: 15-admin-church-outreach-research-management
plan: 09
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - src/app/(admin)/admin/church-outreach/page.tsx
  - src/app/(admin)/admin/church-outreach/layout.tsx
  - src/components/admin/church-outreach/OutreachDashboard.tsx
  - src/components/admin/church-outreach/DiscoverySearch.tsx
  - src/components/admin/church-outreach/ChurchDetailModal.tsx
  - package.json
autonomous: true
requirements:
  - CO-04
  - CO-05
  - CO-14

must_haves:
  truths:
    - "Admin can navigate to /admin/church-outreach from the admin sidebar"
    - "Dashboard tab shows pipeline funnel and summary metrics"
    - "Discovery tab lets admin search by location+radius and view results"
    - "Admin can select discovery results and import them into the CRM"
    - "Admin can click a church to view full details in a modal"
  artifacts:
    - path: "src/app/(admin)/admin/church-outreach/page.tsx"
      provides: "Main church outreach admin page with tab navigation"
    - path: "src/components/admin/church-outreach/OutreachDashboard.tsx"
      provides: "Summary dashboard with pipeline funnel and metrics"
    - path: "src/components/admin/church-outreach/DiscoverySearch.tsx"
      provides: "Discovery search form + results staging area"
    - path: "src/components/admin/church-outreach/ChurchDetailModal.tsx"
      provides: "Church detail/edit modal with activity timeline"
  key_links:
    - from: "src/app/(admin)/admin/church-outreach/page.tsx"
      to: "src/components/admin/church-outreach/OutreachDashboard.tsx"
      via: "React.lazy() tab loading"
      pattern: "React\\.lazy"
    - from: "src/components/admin/church-outreach/DiscoverySearch.tsx"
      to: "/api/admin/church-outreach/discover"
      via: "fetch POST for search"
      pattern: "church-outreach/discover"
---

<objective>
Create the main church outreach admin page with tab navigation, dashboard overview, discovery search UI with results staging, and church detail modal. Install @dnd-kit dependencies for the kanban board (plan 15-10).

Purpose: This is the admin's primary interface for the church outreach CRM.
Output: Admin page with layout, dashboard tab, discovery tab, and church detail modal
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-admin-church-outreach-research-management/15-RESEARCH.md
@.planning/phases/15-admin-church-outreach-research-management/15-04-SUMMARY.md
@src/components/admin/AdminNav.tsx

<interfaces>
<!-- Reference admin page patterns -->
From src/app/(admin)/admin/content-production/page.tsx (reference for multi-tab admin page):
- 'use client' with React.lazy for tab components
- Tab state with useState
- Suspense wrapper for lazy-loaded tabs
- Tab bar with active/inactive styling

From src/components/admin/AdminNav.tsx:
- navItems array with label, href, icon
- Add new item: { label: 'Church Outreach', href: '/admin/church-outreach', icon: Church }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd-kit, create page shell, and update AdminNav</name>
  <files>
    package.json
    src/components/admin/AdminNav.tsx
    src/app/(admin)/admin/church-outreach/layout.tsx
    src/app/(admin)/admin/church-outreach/page.tsx
  </files>
  <action>
**Install:** `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`

**Update AdminNav.tsx:**
- Add import: `Church` from lucide-react (the building icon for church)
- Add new navItem before Analytics: `{ label: 'Church Outreach', href: '/admin/church-outreach', icon: Church }`

**src/app/(admin)/admin/church-outreach/layout.tsx:**
- Simple pass-through layout (children only), same as other admin sub-pages
- Metadata: title "Church Outreach | Admin"

**src/app/(admin)/admin/church-outreach/page.tsx:**
- 'use client'
- 6 tabs: Dashboard, Discovery, Pipeline, Campaigns, Sequences, Reports
- Use React.lazy for each tab component (loaded from components/admin/church-outreach/)
- Tab state with useState, default to 'dashboard'
- Tab bar styled like existing admin tab patterns (underline active tab)
- Suspense fallback with Skeleton
- Each tab component receives no props initially (they fetch their own data)

Tab mapping:
- Dashboard → OutreachDashboard (this plan)
- Discovery → DiscoverySearch (this plan)
- Pipeline → KanbanBoard (plan 15-10)
- Campaigns → CampaignManager (plan 15-11)
- Sequences → SequenceManager (plan 15-11)
- Reports → ReportsView (plan 15-11)

For tabs not yet built (Pipeline, Campaigns, Sequences, Reports), use a simple placeholder component: `<div className="p-8 text-center text-text-muted">Coming soon...</div>`
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Church Outreach page with 6 tabs, AdminNav link, and dnd-kit installed</done>
</task>

<task type="auto">
  <name>Task 2: Create OutreachDashboard, DiscoverySearch, and ChurchDetailModal components</name>
  <files>
    src/components/admin/church-outreach/OutreachDashboard.tsx
    src/components/admin/church-outreach/DiscoverySearch.tsx
    src/components/admin/church-outreach/ChurchDetailModal.tsx
  </files>
  <action>
**OutreachDashboard.tsx:**
- Fetches data from GET /api/admin/church-outreach/reports on mount
- Displays:
  1. **Summary cards** (4 cards in a grid): Total Churches, Total Emails Sent, Open Rate %, Conversions
  2. **Pipeline funnel** -- horizontal bar chart showing count at each stage, color-coded:
     - new_lead: blue, contacted: indigo, engaged: purple, sample_requested: amber, sample_sent: orange, converted: green, lost: gray
     - Use CSS inline widths (percentage of max), same pattern as moderation stats bar chart
  3. **Activity timeline** -- 30-day bar chart (CSS bars, no chart library)
  4. **Top engaged churches** -- table showing church name, emails sent, opens, clicks
- Loading skeleton while fetching
- Error state with retry button

**DiscoverySearch.tsx:**
- Two-section layout:
  1. **Search form** (top): location input, radius slider (5-50 miles), filters input, search button
  2. **Results staging area** (below): list of discovered churches with checkboxes
- Search flow:
  1. Admin fills form, clicks "Search Churches"
  2. POST to /api/admin/church-outreach/discover
  3. Results displayed as cards with: name, address, phone, website, rating
  4. Cards that already exist in CRM show "Already Imported" badge (grayed out, checkbox disabled)
  5. Each non-imported card has a checkbox for selection
  6. "Research Selected" button: for each selected church with a website, POST to /api/admin/church-outreach/discover/scrape
  7. Research results shown inline on the card (pastor name, denomination, youth programs, AI summary)
  8. "Import Selected" button: POST to /api/admin/church-outreach/discover/import
  9. Success toast with count of imported churches
- Loading states during search, research, and import operations
- If no GOOGLE_PLACES_API_KEY configured, show info message with setup instructions

**ChurchDetailModal.tsx:**
- Uses the existing Modal component
- Props: churchId, isOpen, onClose, onUpdate
- Fetches church detail from GET /api/admin/church-outreach/churches/[id] on open
- Two-column layout on desktop, single column on mobile:
  - **Left: Church info** -- editable fields (name, pastor, email, phone, denomination, etc.)
  - **Right: Activity timeline** -- chronological list of activities with icons per type
- Pipeline stage selector: dropdown showing all stages with color dots
- Notes textarea for admin notes
- Save button: PUT /api/admin/church-outreach/churches/[id]
- Delete button: with confirmation, DELETE /api/admin/church-outreach/churches/[id]
- Youth programs shown as tags (removable chips)
- Social media links as clickable URLs
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Dashboard shows pipeline funnel and metrics; Discovery lets admin search, research, and import churches; Detail modal shows full church profile with activity history</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /admin/church-outreach page renders with tab navigation
- AdminNav shows "Church Outreach" link
- Dashboard fetches and displays report data
- Discovery search → results → research → import flow works
</verification>

<success_criteria>
- Admin can access church outreach from sidebar navigation
- Dashboard displays pipeline funnel, summary metrics, and activity timeline
- Discovery search returns churches from Google Places API with duplicate detection
- Research enriches church cards with AI-generated profiles
- Import creates CRM records from selected discoveries
- Church detail modal shows full profile with editable fields and activity history
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-church-outreach-research-management/15-09-SUMMARY.md`
</output>
