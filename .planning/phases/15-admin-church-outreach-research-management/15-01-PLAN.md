---
phase: 15-admin-church-outreach-research-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/migrations/105-create-churches.cjs
  - src/lib/db/migrations/106-create-church-activities.cjs
  - src/lib/db/migrations/107-create-outreach-templates.cjs
  - src/lib/db/migrations/108-create-outreach-campaigns.cjs
  - src/lib/db/migrations/109-create-outreach-emails.cjs
  - src/lib/db/migrations/110-create-outreach-unsubscribes.cjs
  - src/lib/db/migrations/111-create-drip-sequences.cjs
  - src/lib/db/migrations/112-create-drip-steps.cjs
  - src/lib/db/migrations/113-create-drip-enrollments.cjs
  - src/lib/db/migrations/114-create-sample-shipments.cjs
  - src/lib/db/migrations/115-create-church-conversions.cjs
  - src/lib/db/models/Church.ts
  - src/lib/db/models/ChurchActivity.ts
  - src/lib/db/models/OutreachTemplate.ts
  - src/lib/db/models/OutreachCampaign.ts
  - src/lib/db/models/OutreachEmail.ts
  - src/lib/db/models/OutreachUnsubscribe.ts
  - src/lib/db/models/DripSequence.ts
  - src/lib/db/models/DripStep.ts
  - src/lib/db/models/DripEnrollment.ts
  - src/lib/db/models/SampleShipment.ts
  - src/lib/db/models/ChurchConversion.ts
  - src/lib/db/models/index.ts
autonomous: true
requirements:
  - CO-01
  - CO-02
  - CO-03

must_haves:
  truths:
    - "All 11 church outreach tables exist in the database after running migrations"
    - "All 11 Sequelize models are registered in the models index with correct associations"
    - "Pipeline stages are defined as a constant ENUM with 7 stages"
  artifacts:
    - path: "src/lib/db/migrations/105-create-churches.cjs"
      provides: "Churches table with pipeline_stage ENUM, full address fields, JSON columns"
    - path: "src/lib/db/models/Church.ts"
      provides: "Church model with pipeline_stage ENUM type"
    - path: "src/lib/db/models/index.ts"
      provides: "All 11 new models imported and associations wired"
  key_links:
    - from: "src/lib/db/models/Church.ts"
      to: "src/lib/db/models/ChurchActivity.ts"
      via: "hasMany/belongsTo church_id FK"
      pattern: "Church\\.hasMany.*ChurchActivity"
    - from: "src/lib/db/models/OutreachCampaign.ts"
      to: "src/lib/db/models/OutreachEmail.ts"
      via: "hasMany/belongsTo campaign_id FK"
      pattern: "OutreachCampaign\\.hasMany.*OutreachEmail"
    - from: "src/lib/db/models/DripSequence.ts"
      to: "src/lib/db/models/DripStep.ts"
      via: "hasMany/belongsTo sequence_id FK"
      pattern: "DripSequence\\.hasMany.*DripStep"
---

<objective>
Create the complete database foundation for the church outreach CRM -- 11 new tables covering churches, activity logs, email templates, campaigns, individual emails, unsubscribes, drip sequences, drip steps, drip enrollments, sample shipments, and church conversions. All models registered with associations.

Purpose: Every subsequent plan depends on these models existing. This is the foundation layer.
Output: 11 migration files, 11 model files, updated models/index.ts
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-admin-church-outreach-research-management/15-CONTEXT.md
@.planning/phases/15-admin-church-outreach-research-management/15-RESEARCH.md

<interfaces>
<!-- Existing model pattern to follow -->
From src/lib/db/models/Announcement.ts (reference for model structure):
- DataTypes, Model, Optional from sequelize
- Interface with Attributes and CreationAttributes
- init() with sequelize instance, tableName, timestamps

From src/lib/db/models/index.ts (reference for registration):
- Import model at top
- Wire associations in dedicated section
- Export from final export block

Migration pattern (from existing .cjs files):
- module.exports = { async up(queryInterface, Sequelize) {}, async down(queryInterface) {} }
- Use queryInterface.createTable / queryInterface.dropTable
- Add indexes with queryInterface.addIndex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 11 database migrations</name>
  <files>
    src/lib/db/migrations/105-create-churches.cjs
    src/lib/db/migrations/106-create-church-activities.cjs
    src/lib/db/migrations/107-create-outreach-templates.cjs
    src/lib/db/migrations/108-create-outreach-campaigns.cjs
    src/lib/db/migrations/109-create-outreach-emails.cjs
    src/lib/db/migrations/110-create-outreach-unsubscribes.cjs
    src/lib/db/migrations/111-create-drip-sequences.cjs
    src/lib/db/migrations/112-create-drip-steps.cjs
    src/lib/db/migrations/113-create-drip-enrollments.cjs
    src/lib/db/migrations/114-create-sample-shipments.cjs
    src/lib/db/migrations/115-create-church-conversions.cjs
  </files>
  <action>
Create 11 Sequelize CLI migration files (.cjs) following the established project pattern.

**105-create-churches.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- google_place_id: STRING(255), unique, allowNull: true
- name: STRING(255), allowNull: false
- pastor_name: STRING(255), allowNull: true
- staff_names: JSON, allowNull: true (array of strings)
- denomination: STRING(100), allowNull: true
- congregation_size_estimate: STRING(50), allowNull: true (e.g., "small <100", "medium 100-500", "large 500+")
- youth_programs: JSON, allowNull: true (array: ["AWANA", "VBS", "Youth Night"])
- service_times: JSON, allowNull: true (array of strings)
- website_url: STRING(500), allowNull: true
- social_media: JSON, allowNull: true (object: {facebook, instagram, etc.})
- contact_email: STRING(255), allowNull: true
- contact_phone: STRING(50), allowNull: true
- address_line1: STRING(255), allowNull: true
- address_line2: STRING(255), allowNull: true
- city: STRING(100), allowNull: true
- state: STRING(50), allowNull: true
- zip_code: STRING(20), allowNull: true
- country: STRING(50), defaultValue: 'US'
- latitude: DECIMAL(10, 7), allowNull: true
- longitude: DECIMAL(10, 7), allowNull: true
- pipeline_stage: ENUM('new_lead', 'contacted', 'engaged', 'sample_requested', 'sample_sent', 'converted', 'lost'), defaultValue: 'new_lead'
- ai_summary: TEXT, allowNull: true
- source: ENUM('google_places', 'manual', 'sample_request'), defaultValue: 'manual'
- notes: TEXT, allowNull: true
- created_at, updated_at: DATE
- Indexes: pipeline_stage, zip_code, state, google_place_id (already unique)

**106-create-church-activities.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: false, references churches(id), ON DELETE CASCADE
- activity_type: ENUM('stage_change', 'email_sent', 'email_opened', 'email_clicked', 'note_added', 'sample_shipped', 'converted', 'created', 'scrape_completed', 'ai_researched')
- description: TEXT, allowNull: true
- metadata: JSON, allowNull: true
- admin_id: INTEGER, allowNull: true, references users(id), ON DELETE SET NULL
- created_at: DATE (no updated_at -- append-only)
- Indexes: (church_id, created_at), (activity_type)

**107-create-outreach-templates.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- name: STRING(255), allowNull: false
- subject: STRING(500), allowNull: false
- html_body: TEXT('long'), allowNull: false
- merge_fields: JSON, allowNull: true (available fields list)
- is_default: BOOLEAN, defaultValue: false
- created_at, updated_at: DATE

**108-create-outreach-campaigns.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- name: STRING(255), allowNull: false
- template_id: INTEGER, allowNull: false, references outreach_templates(id), ON DELETE RESTRICT
- filter_criteria: JSON, allowNull: true (stage/state/denomination filters)
- status: ENUM('draft', 'sending', 'sent', 'cancelled'), defaultValue: 'draft'
- sent_count: INTEGER, defaultValue: 0
- open_count: INTEGER, defaultValue: 0
- click_count: INTEGER, defaultValue: 0
- created_by: INTEGER, allowNull: false, references users(id), ON DELETE RESTRICT
- sent_at: DATE, allowNull: true
- created_at, updated_at: DATE
- Indexes: (status), (created_by)

**109-create-outreach-emails.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: false, references churches(id), ON DELETE CASCADE
- campaign_id: INTEGER, allowNull: true, references outreach_campaigns(id), ON DELETE SET NULL
- drip_enrollment_id: INTEGER, allowNull: true, references drip_enrollments(id), ON DELETE SET NULL
- template_id: INTEGER, allowNull: false, references outreach_templates(id), ON DELETE RESTRICT
- to_email: STRING(255), allowNull: false
- subject: STRING(500), allowNull: false
- status: ENUM('queued', 'sent', 'bounced', 'opened', 'clicked'), defaultValue: 'queued'
- tracking_id: STRING(36), unique, allowNull: false (UUID)
- sent_at: DATE, allowNull: true
- opened_at: DATE, allowNull: true
- clicked_at: DATE, allowNull: true
- created_at: DATE (no updated_at)
- Indexes: (church_id), (campaign_id), (tracking_id unique), (status)

**110-create-outreach-unsubscribes.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: true, references churches(id), ON DELETE SET NULL
- email: STRING(255), allowNull: false, unique
- unsubscribed_at: DATE, allowNull: false
- created_at: DATE (no updated_at)

**111-create-drip-sequences.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- name: STRING(255), allowNull: false
- description: TEXT, allowNull: true
- trigger: ENUM('manual', 'sample_shipped', 'stage_change'), defaultValue: 'manual'
- is_active: BOOLEAN, defaultValue: true
- created_at, updated_at: DATE

**112-create-drip-steps.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- sequence_id: INTEGER, allowNull: false, references drip_sequences(id), ON DELETE CASCADE
- step_order: INTEGER, allowNull: false
- template_id: INTEGER, allowNull: false, references outreach_templates(id), ON DELETE RESTRICT
- delay_days: INTEGER, allowNull: false, defaultValue: 1
- created_at, updated_at: DATE
- Indexes: (sequence_id, step_order) unique

**113-create-drip-enrollments.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: false, references churches(id), ON DELETE CASCADE
- sequence_id: INTEGER, allowNull: false, references drip_sequences(id), ON DELETE CASCADE
- current_step: INTEGER, defaultValue: 0
- status: ENUM('active', 'paused', 'completed', 'cancelled'), defaultValue: 'active'
- next_step_at: DATE, allowNull: true
- enrolled_at: DATE, allowNull: false
- completed_at: DATE, allowNull: true
- created_at, updated_at: DATE
- Indexes: (church_id, sequence_id), (status, next_step_at)

**114-create-sample-shipments.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: false, references churches(id), ON DELETE CASCADE
- ship_date: DATEONLY, allowNull: false
- tracking_number: STRING(100), allowNull: true
- carrier: ENUM('usps', 'ups', 'fedex', 'other'), defaultValue: 'usps'
- bracelet_type: STRING(255), allowNull: true
- quantity: INTEGER, allowNull: true
- shipping_address: TEXT, allowNull: true
- notes: TEXT, allowNull: true
- created_by: INTEGER, allowNull: false, references users(id), ON DELETE RESTRICT
- created_at, updated_at: DATE
- Indexes: (church_id)

**115-create-church-conversions.cjs:**
- id: INTEGER, autoIncrement, primaryKey
- church_id: INTEGER, allowNull: false, references churches(id), ON DELETE CASCADE, unique
- order_date: DATEONLY, allowNull: true
- estimated_size: INTEGER, allowNull: true (bracelet count)
- revenue_estimate: DECIMAL(10, 2), allowNull: true
- notes: TEXT, allowNull: true
- created_by: INTEGER, allowNull: false, references users(id), ON DELETE RESTRICT
- created_at: DATE (no updated_at -- append-only)

Note: migration 109 references drip_enrollments(id) which is created in 113. To avoid FK ordering issues, add the drip_enrollment_id FK constraint in migration 109 WITHOUT a foreign key reference, then add the FK constraint at the end of migration 113 via queryInterface.addConstraint. OR simply make the FK reference in 109 and ensure migrations run in numeric order (Sequelize guarantees this).

Actually, the simpler approach: migration 109 should NOT reference drip_enrollments since that table doesn't exist yet. Instead, create the drip_enrollment_id column in 109 as INTEGER allowNull: true with NO references. Then in migration 113, after creating drip_enrollments, add the FK constraint via queryInterface.addConstraint on outreach_emails.drip_enrollment_id.
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx sequelize-cli db:migrate</automated>
  </verify>
  <done>All 11 tables created successfully in the database with correct columns, types, indexes, and foreign key constraints</done>
</task>

<task type="auto">
  <name>Task 2: Create all 11 Sequelize models and register associations</name>
  <files>
    src/lib/db/models/Church.ts
    src/lib/db/models/ChurchActivity.ts
    src/lib/db/models/OutreachTemplate.ts
    src/lib/db/models/OutreachCampaign.ts
    src/lib/db/models/OutreachEmail.ts
    src/lib/db/models/OutreachUnsubscribe.ts
    src/lib/db/models/DripSequence.ts
    src/lib/db/models/DripStep.ts
    src/lib/db/models/DripEnrollment.ts
    src/lib/db/models/SampleShipment.ts
    src/lib/db/models/ChurchConversion.ts
    src/lib/db/models/index.ts
  </files>
  <action>
Create 11 TypeScript Sequelize model files following established patterns (see Announcement.ts as reference).

Each model needs:
1. TypeScript interface with Attributes and CreationAttributes (using Optional for auto-generated fields)
2. Class extending Model implementing the interface
3. Static init() call with column definitions matching migration
4. tableName, timestamps config matching migration (some have updatedAt: false)

**Pipeline stage constant** -- define in Church.ts:
```typescript
export const PIPELINE_STAGES = ['new_lead', 'contacted', 'engaged', 'sample_requested', 'sample_sent', 'converted', 'lost'] as const;
export type PipelineStage = typeof PIPELINE_STAGES[number];
```

**Append-only models** (updatedAt: false): ChurchActivity, OutreachEmail, OutreachUnsubscribe, ChurchConversion

**Models index update** -- add to src/lib/db/models/index.ts:
1. Import all 11 new models at the top (after existing imports)
2. Add associations section "Phase 15: Church Outreach CRM Associations":

Key associations:
- Church hasMany ChurchActivity (church_id), cascade
- Church hasMany OutreachEmail (church_id), cascade
- Church hasMany DripEnrollment (church_id), cascade
- Church hasMany SampleShipment (church_id), cascade
- Church hasOne ChurchConversion (church_id), cascade
- User hasMany ChurchActivity (admin_id) as 'churchActivities'
- OutreachTemplate hasMany OutreachCampaign (template_id)
- OutreachTemplate hasMany OutreachEmail (template_id)
- OutreachTemplate hasMany DripStep (template_id)
- OutreachCampaign hasMany OutreachEmail (campaign_id)
- OutreachCampaign belongsTo User (created_by) as 'creator'
- DripSequence hasMany DripStep (sequence_id), cascade
- DripSequence hasMany DripEnrollment (sequence_id), cascade
- DripEnrollment hasMany OutreachEmail (drip_enrollment_id)
- DripEnrollment belongsTo Church (church_id)
- DripEnrollment belongsTo DripSequence (sequence_id)
- SampleShipment belongsTo User (created_by) as 'creator'
- ChurchConversion belongsTo User (created_by) as 'creator'

3. Export all 11 new models from the export block
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 11 models compile without TypeScript errors, are registered in models/index.ts with correct associations, and are exported</done>
</task>

</tasks>

<verification>
- `npx sequelize-cli db:migrate` runs without errors
- `npx tsc --noEmit` passes (no TypeScript errors from new models)
- All 11 tables visible in database
- Models index exports all 11 new models
</verification>

<success_criteria>
- 11 migration files created at migrations/105 through migrations/115
- 11 model files created in src/lib/db/models/
- models/index.ts updated with imports, associations, and exports for all 11 new models
- Database tables created with correct columns, types, and foreign keys
- PIPELINE_STAGES constant exported from Church.ts for use by API routes and UI
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-church-outreach-research-management/15-01-SUMMARY.md`
</output>
