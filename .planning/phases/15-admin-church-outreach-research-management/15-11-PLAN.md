---
phase: 15-admin-church-outreach-research-management
plan: 11
type: execute
wave: 6
depends_on: [15-05, 15-06, 15-07, 15-09]
files_modified:
  - src/components/admin/church-outreach/CampaignManager.tsx
  - src/components/admin/church-outreach/TemplateEditor.tsx
  - src/components/admin/church-outreach/SequenceManager.tsx
  - src/components/admin/church-outreach/SequenceBuilder.tsx
  - src/components/admin/church-outreach/ReportsView.tsx
  - src/components/admin/church-outreach/SampleShipmentForm.tsx
  - src/components/admin/church-outreach/ConversionForm.tsx
autonomous: true
requirements:
  - CO-06
  - CO-09
  - CO-10
  - CO-11
  - CO-12
  - CO-14

must_haves:
  truths:
    - "Admin can create and send email campaigns with template selection and church filtering"
    - "Admin can create and edit email templates with merge field insertion"
    - "Admin can create and manage drip sequences with configurable steps"
    - "Admin can view conversion reports with pipeline funnel and revenue estimates"
    - "Admin can log sample shipments and mark churches as converted"
  artifacts:
    - path: "src/components/admin/church-outreach/CampaignManager.tsx"
      provides: "Campaign list, creation, and send UI"
    - path: "src/components/admin/church-outreach/TemplateEditor.tsx"
      provides: "HTML template editor with merge field buttons"
    - path: "src/components/admin/church-outreach/SequenceManager.tsx"
      provides: "Drip sequence list and management"
    - path: "src/components/admin/church-outreach/ReportsView.tsx"
      provides: "Conversion reports dashboard"
  key_links:
    - from: "src/components/admin/church-outreach/CampaignManager.tsx"
      to: "/api/admin/church-outreach/campaigns"
      via: "fetch for campaign CRUD and send"
      pattern: "church-outreach/campaigns"
    - from: "src/components/admin/church-outreach/SequenceManager.tsx"
      to: "/api/admin/church-outreach/sequences"
      via: "fetch for sequence CRUD"
      pattern: "church-outreach/sequences"
---

<objective>
Create the remaining admin UI components: campaign manager with template editor, drip sequence builder, reports view, and sample/conversion forms. These fill in the Campaigns, Sequences, and Reports tabs.

Purpose: These components complete the admin CRM workflow -- from email outreach to conversion tracking.
Output: 7 admin UI components
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-admin-church-outreach-research-management/15-RESEARCH.md
@.planning/phases/15-admin-church-outreach-research-management/15-05-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-06-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-07-SUMMARY.md
@.planning/phases/15-admin-church-outreach-research-management/15-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CampaignManager and TemplateEditor components</name>
  <files>
    src/components/admin/church-outreach/CampaignManager.tsx
    src/components/admin/church-outreach/TemplateEditor.tsx
  </files>
  <action>
**TemplateEditor.tsx:**
- Props: template? (for editing), onSave callback, onCancel callback
- Opens in a Modal
- Fields:
  - Template name (text input)
  - Email subject (text input with merge field insertion)
  - HTML body (large textarea, ~20 rows)
- **Merge field insertion buttons:** Row of buttons for each MERGE_FIELD ({PastorName}, {ChurchName}, etc.)
  - Clicking a button inserts the merge field at the cursor position in the active textarea (subject or body)
  - Use useRef for textarea refs and selectionStart/selectionEnd for cursor position
- **Preview panel:** Side-by-side or toggle view showing the rendered HTML
  - Uses a sample church data object for preview rendering
  - Renders in an iframe or div with dangerouslySetInnerHTML (admin-only, trusted content)
- Save button: POST (create) or PUT (update) to templates endpoint
- No rich text editor library -- just a plain HTML textarea with merge field buttons (per CONTEXT decision: "Custom textarea + merge fields" over heavy email editor libraries)

**CampaignManager.tsx:**
- Two-section layout:
  1. **Campaign list** (default view): table with name, template, status, sent/open/click counts, date
     - Status badge coloring: draft=blue, sending=amber, sent=green, cancelled=gray
     - Click row to view campaign detail (sent emails list)
  2. **Create campaign** (toggle view): form for new campaign

- **Create campaign form:**
  - Campaign name (text input)
  - Template selector (dropdown, fetches from GET /templates)
  - "Create New Template" button → opens TemplateEditor modal
  - Filter criteria:
    - Pipeline stages: multi-select checkboxes
    - States: multi-select or comma-separated input
    - Denominations: multi-select or comma-separated input
  - "Preview Recipients" button: shows count of matching churches (POST to campaigns with preview)
  - "Save as Draft" button: creates campaign with status='draft'
  - "Send Now" button: creates campaign then immediately sends (POST action=send)
  - Confirmation dialog before sending: "This will send to X churches. Continue?"
  - Loading state during send with progress indication

- **Campaign detail view** (when clicking a campaign):
  - Campaign stats (sent, opened, clicked rates)
  - Email list: table of OutreachEmail records with church name, status, timestamps
  - Back button to return to list
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>CampaignManager with create/send flow and TemplateEditor with merge field insertion and preview working</done>
</task>

<task type="auto">
  <name>Task 2: Create SequenceManager, ReportsView, SampleShipmentForm, and ConversionForm</name>
  <files>
    src/components/admin/church-outreach/SequenceManager.tsx
    src/components/admin/church-outreach/SequenceBuilder.tsx
    src/components/admin/church-outreach/ReportsView.tsx
    src/components/admin/church-outreach/SampleShipmentForm.tsx
    src/components/admin/church-outreach/ConversionForm.tsx
  </files>
  <action>
**SequenceBuilder.tsx:**
- Props: sequence? (for editing), onSave callback, onCancel callback
- Opens in a Modal
- Fields:
  - Sequence name (text input)
  - Description (textarea)
  - Trigger type: radio buttons (Manual, Sample Shipped, Stage Change)
  - Steps list (dynamic, add/remove):
    - Each step: template selector (dropdown) + delay_days (number input, label "Days after previous step")
    - "Add Step" button to append a new step
    - Remove button (X) on each step
    - Steps numbered 1, 2, 3...
- Save button: POST (create) or PUT (update) to sequences endpoint

**SequenceManager.tsx:**
- Two-section layout:
  1. **Sequence list:** cards showing name, trigger, step count, active enrollments count, is_active toggle
     - Active/inactive toggle: PUT to update is_active
     - Click to edit (opens SequenceBuilder)
     - Delete button with confirmation (409 handling for active enrollments)
  2. **Enrollment view** (separate sub-section):
     - Shows active enrollments for a selected sequence
     - Church name, current step, next_step_at, status
     - Pause/Resume/Cancel buttons per enrollment
     - "Enroll Church" button → opens church selector (simple search input + results)
  - "Create Sequence" button → opens SequenceBuilder modal

**ReportsView.tsx:**
- Fetches from GET /api/admin/church-outreach/reports on mount
- Displays:
  1. **Summary cards row:** Total Churches, Samples Shipped, Conversions, Revenue Estimate (formatted as currency)
  2. **Conversion funnel:** Visual pipeline funnel (horizontal stacked bars or cascading bars)
     - Shows: Total → Contacted → Engaged → Samples → Converted
     - Percentage at each stage relative to total
  3. **Email performance:** Open rate, click rate, bounce rate as percentage cards
  4. **Activity timeline:** 30-day CSS bar chart (reuse pattern from OutreachDashboard but with more detail)
  5. **Top engaged churches table:** Name, total opens, total clicks, pipeline stage, last activity
- Loading skeleton, error handling, refresh button

**SampleShipmentForm.tsx:**
- Props: churchId, churchName, onSuccess callback
- Opens in a Modal (triggered from ChurchDetailModal or Pipeline context menu)
- Fields: ship date (date input, default today), tracking number, carrier (dropdown: USPS/UPS/FedEx/Other), bracelet type, quantity, shipping address (textarea), notes
- Submit: POST /api/admin/church-outreach/samples
- Success toast: "Sample shipment logged for {churchName}"

**ConversionForm.tsx:**
- Props: churchId, churchName, onSuccess callback
- Opens in a Modal
- Fields: order date (date input), estimated size (number), revenue estimate (number, formatted as currency), notes
- Submit: POST /api/admin/church-outreach/conversions
- Handle 409 (already converted): show info message
- Success toast: "Conversion recorded for {churchName}"

**Wire up in page.tsx:**
- Update the church-outreach page.tsx to replace placeholder tabs with:
  - Campaigns → CampaignManager
  - Sequences → SequenceManager
  - Reports → ReportsView
  </action>
  <verify>
    <automated>cd /Applications/XAMPP/xamppfiles/htdocs/FreeLumaPlatform && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All remaining admin UI components (sequences, reports, samples, conversions) built and wired into tabs</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 6 tabs on the church outreach page render their respective components
- No placeholder tabs remaining
</verification>

<success_criteria>
- Campaign creation with template selection, church filtering, preview, and send flow
- Template editor with merge field insertion buttons and HTML preview
- Sequence builder with dynamic step management
- Sequence manager with enrollment view and pause/resume/cancel
- Reports view with funnel visualization, email metrics, and top engaged churches
- Sample shipment and conversion forms with modal UX
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-church-outreach-research-management/15-11-SUMMARY.md`
</output>
