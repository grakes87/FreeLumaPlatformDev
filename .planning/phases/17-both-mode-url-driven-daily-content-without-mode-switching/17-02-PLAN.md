---
phase: 17-both-mode-url-driven-daily-content-without-mode-switching
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/context/ViewModeContext.tsx
  - src/app/(app)/layout.tsx
  - src/components/layout/BottomNav.tsx
  - src/components/layout/TopBar.tsx
  - src/components/layout/CreatePicker.tsx
  - src/components/daily/DailyFeed.tsx
  - src/components/tutorial/TutorialProvider.tsx
  - src/app/(app)/page.tsx
  - src/app/(app)/positivity/page.tsx
autonomous: true

must_haves:
  truths:
    - "Both-mode users see their active view mode's content (not 'both' raw value) everywhere in the app"
    - "Bible-only and Positivity-only users see zero behavioral changes"
    - "Active view mode persists in localStorage across page refreshes for Both users"
    - "URL /positivity sets initial view mode to positivity for Both users"
    - "Root URL / sets initial view mode to bible for Both users"
    - "Prayer Wall tab shows/hides based on effective view mode, not user.mode"
    - "Nav icons use effective view mode for image paths"
    - "If a mode has no content at all, Both users are auto-switched back to Bible with a toast message"
  artifacts:
    - path: "src/context/ViewModeContext.tsx"
      provides: "ViewModeProvider and useViewMode hook"
      exports: ["ViewModeProvider", "useViewMode"]
      min_lines: 40
    - path: "src/app/(app)/layout.tsx"
      provides: "ViewModeProvider wrapping the app shell"
      contains: "ViewModeProvider"
    - path: "src/components/layout/BottomNav.tsx"
      provides: "BottomNav reading effectiveMode from ViewModeContext"
      contains: "useViewMode"
    - path: "src/components/layout/TopBar.tsx"
      provides: "TopBar reading effectiveMode from ViewModeContext"
      contains: "useViewMode"
    - path: "src/components/daily/DailyFeed.tsx"
      provides: "DailyFeed using effectiveMode for content fetching and empty-content auto-switch"
      contains: "useViewMode"
  key_links:
    - from: "src/context/ViewModeContext.tsx"
      to: "localStorage"
      via: "fl_view_mode key"
      pattern: "localStorage.*fl_view_mode"
    - from: "src/app/(app)/layout.tsx"
      to: "src/context/ViewModeContext.tsx"
      via: "ViewModeProvider with initialMode from pathname"
      pattern: "ViewModeProvider.*initialMode"
    - from: "src/components/layout/BottomNav.tsx"
      to: "src/context/ViewModeContext.tsx"
      via: "useViewMode().effectiveMode"
      pattern: "useViewMode"
    - from: "src/components/daily/DailyFeed.tsx"
      to: "src/context/ViewModeContext.tsx"
      via: "useViewMode().effectiveMode for useDailyFeed + setViewMode for empty-content fallback"
      pattern: "useViewMode"
---

<objective>
Create the ViewModeContext that resolves the "effective rendering mode" and update all consumer components to use it instead of reading user.mode directly.

Purpose: This is the central state management for Both mode. Without it, components would still read 'both' from user.mode and break. The context resolves 'both' into either 'bible' or 'positivity' based on localStorage and URL.
Output: ViewModeContext.tsx, updated layout.tsx, and all 7 consumer components updated.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-CONTEXT.md
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-RESEARCH.md
@src/app/(app)/layout.tsx
@src/components/layout/BottomNav.tsx
@src/components/layout/TopBar.tsx
@src/components/layout/CreatePicker.tsx
@src/components/daily/DailyFeed.tsx
@src/components/tutorial/TutorialProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ViewModeContext and integrate into app layout</name>
  <files>
    src/context/ViewModeContext.tsx
    src/app/(app)/layout.tsx
    src/app/(app)/page.tsx
    src/app/(app)/positivity/page.tsx
  </files>
  <action>
    1. Create `src/context/ViewModeContext.tsx` following the exact pattern from 17-RESEARCH.md Pattern 1:
       - `'use client'` directive
       - Type: `EffectiveMode = 'bible' | 'positivity'` (never 'both')
       - Context value: `{ effectiveMode, isBothMode, setViewMode }`
       - `STORAGE_KEY = 'fl_view_mode'`
       - `ViewModeProvider` component:
         - Reads `user` from `useAuth()`
         - `isBothMode = user?.mode === 'both'` (naturally false when user is null/guest)
         - State initialized from: `initialMode` prop (URL) > localStorage > 'bible'
         - `useEffect` to update state when `initialMode` changes (URL navigation)
         - `setViewMode` writes to localStorage and updates state (only for Both users)
         - Resolution: non-both users always get `user?.mode` or 'bible'; both users get viewMode state
       - `useViewMode()` hook wrapping useContext

    2. Update `src/app/(app)/layout.tsx`:
       - Import `ViewModeProvider` from `@/context/ViewModeContext`
       - In `AuthenticatedLayout`, the authenticated user section (around line 132-142) -- wrap the content inside `<SocketProvider>` block with `<ViewModeProvider>`.
       - The `initialMode` prop must be derived from pathname: `pathname === '/positivity' ? 'positivity' : 'bible'`
       - Placement: `<ViewModeProvider initialMode={...}>` wraps `<TutorialProvider>` and its children.
       - The `DailyTranslationProvider` used in `GuestDailyWrapper` does NOT need ViewModeProvider (guests are never Both mode).

    3. Update `src/app/(app)/page.tsx` -- remove the mode prop pass to DailyFeed (DailyFeed will read from ViewModeContext instead):
       Keep as `<DailyFeed />` (no change needed -- it already doesn't pass mode).

    4. Update `src/app/(app)/positivity/page.tsx`:
       - Currently passes `mode="positivity"` to DailyFeed.
       - Keep the `mode="positivity"` prop as fallback for guests. DailyFeed will use ViewModeContext for authenticated users and the prop for guests.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    ViewModeProvider is imported and rendered in layout.tsx's AuthenticatedLayout.
    `grep -n "ViewModeProvider" src/app/\(app\)/layout.tsx` shows the provider in the tree.
  </verify>
  <done>
    ViewModeContext created. Layout wraps authenticated users with ViewModeProvider. initialMode derived from URL pathname.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer components to use useViewMode()</name>
  <files>
    src/components/layout/BottomNav.tsx
    src/components/layout/TopBar.tsx
    src/components/layout/CreatePicker.tsx
    src/components/daily/DailyFeed.tsx
    src/components/tutorial/TutorialProvider.tsx
  </files>
  <action>
    For each file, import `useViewMode` from `@/context/ViewModeContext` and replace direct `user?.mode` reads with `effectiveMode` from the context.

    **BottomNav.tsx:**
    - Line 59: Replace `const mode = user?.mode ?? 'bible'` with `const { effectiveMode } = useViewMode(); const mode = effectiveMode;`
    - The rest of the file already uses `mode` variable -- no further changes needed.
    - This fixes: tab filtering (Prayer Wall visibility), nav icon paths (`tab-1-${mode}.png`), workshop label.

    **TopBar.tsx:**
    - Line 51: Replace `const isBibleMode = user?.mode === 'bible' || guestOnBibleRoute` with:
      ```
      const { effectiveMode, isBothMode } = useViewMode();
      const isBibleMode = effectiveMode === 'bible' || guestOnBibleRoute;
      ```
    - The `showVerseModeToggle` logic on line 52 already uses `isBibleMode` -- no change needed.
    - NOTE: For guests (no ViewModeContext wrapping them), `useViewMode()` returns default values (`effectiveMode: 'bible', isBothMode: false`). This is safe because TopBar's `transparent` prop gates guest mode, and the `guestOnBibleRoute` fallback handles it.
    - IMPORTANT: TopBar is rendered inside `GuestDailyWrapper` too, where there is no ViewModeProvider. Add a safety wrapper: use `useContext(ViewModeContext)` with a null check or ensure the default context value (`effectiveMode: 'bible'`) is sensible for guests.

    **CreatePicker.tsx:**
    - Line 22: Replace `const wl = workshopLabel(user?.mode)` with:
      ```
      const { effectiveMode } = useViewMode();
      const wl = workshopLabel(effectiveMode);
      ```

    **DailyFeed.tsx:**
    - Add `import { useViewMode } from '@/context/ViewModeContext'`
    - Add `import { useToast } from '@/components/ui/Toast'`
    - Line 29-31: Replace `useDailyFeed(user?.mode || mode, user?.language)` with:
      ```
      const { effectiveMode: viewEffectiveMode, isBothMode, setViewMode } = useViewMode();
      const toast = useToast();
      // Use ViewModeContext for authenticated users, fallback to mode prop for guests
      const resolvedMode = user ? viewEffectiveMode : (mode || 'bible');
      const { days, loading, ... } = useDailyFeed(resolvedMode, user?.language);
      ```
    - Line 35: Replace `const effectiveMode = user?.mode || mode || 'bible'` with `const effectiveMode = resolvedMode`
    - Line 36: `isBibleMode` now uses the resolved effectiveMode (already correct after the above change)
    - Add scroll-to-top on mode change: Add a `useEffect` that watches `resolvedMode` and calls `getScrollContainer()?.scrollTo(0, 0)` when it changes (per CONTEXT.md: "Scroll position resets to top (today) on mode switch"). Use a ref to track previous mode and only scroll on actual change.
    - **Empty-content auto-switch back to Bible (CONTEXT.md locked decision):** Add a `useEffect` that watches `[days, loading, resolvedMode, isBothMode]`. When ALL of these conditions are true:
      1. `!loading` (fetch has completed)
      2. `days.length === 0` (no content returned)
      3. `isBothMode` (user is in Both mode)
      4. `resolvedMode !== 'bible'` (currently viewing a non-Bible mode)

      Then call `setViewMode('bible')` and `toast.info('No content available for this mode. Switching to Bible.')`.
      This handles the edge case where an entire mode has no daily content at all -- the user is automatically switched back to Bible with a brief informational toast. Guard with a ref to prevent repeated firing.

    **TutorialProvider.tsx:**
    - Line 51: Replace `const userMode = user?.mode ?? 'bible'` with:
      ```
      const { effectiveMode } = useViewMode();
      const userMode = effectiveMode;
      ```
    - Line 57: The `bibleOnly` filter already uses `userMode` -- no further changes.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `grep -rn "user?\.mode" src/components/layout/BottomNav.tsx src/components/layout/TopBar.tsx src/components/layout/CreatePicker.tsx src/components/daily/DailyFeed.tsx src/components/tutorial/TutorialProvider.tsx` returns zero matches (all replaced with useViewMode).
    `grep -n "setViewMode.*bible" src/components/daily/DailyFeed.tsx` shows the empty-content auto-switch logic.
  </verify>
  <done>
    All 5 consumer components use useViewMode().effectiveMode instead of user?.mode. Prayer Wall tab, nav icons, workshop labels, daily feed content, verse mode toggle, and tutorial all respond to the active view mode for Both users. Bible-only and Positivity-only users see zero changes. Empty-content edge case auto-switches Both users back to Bible with a toast.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. No file references `user?.mode` for rendering decisions (only user.mode for auth/profile display is acceptable)
3. ViewModeProvider wraps the authenticated layout tree
4. Default context values are safe for guest rendering (TopBar in GuestDailyWrapper)
5. DailyFeed resets scroll on mode change
6. DailyFeed auto-switches to Bible with toast when a mode returns zero content for Both users
</verification>

<success_criteria>
- ViewModeContext provides effectiveMode ('bible' or 'positivity', never 'both') to the component tree
- Both users in 'bible' view see Prayer Wall tab, Bible nav icons, daily verse content
- Both users in 'positivity' view see no Prayer Wall tab, Positivity nav icons, positivity content
- Bible-only and Positivity-only users are completely unaffected
- localStorage persists active view mode across page refreshes
- URL determines initial mode (/positivity -> positivity, / -> bible)
- If a mode has no content, Both users auto-switch to Bible with an info toast
</success_criteria>

<output>
After completion, create `.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-02-SUMMARY.md`
</output>
