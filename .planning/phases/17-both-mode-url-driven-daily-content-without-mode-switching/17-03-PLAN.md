---
phase: 17-both-mode-url-driven-daily-content-without-mode-switching
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/app/(app)/settings/page.tsx
  - src/components/onboarding/ModeSelector.tsx
autonomous: true

must_haves:
  truths:
    - "Settings page shows three mode options: Bible, Positivity, Both"
    - "Selecting 'Both' does NOT show a confirmation dialog (it's additive, non-destructive)"
    - "Selecting 'Both' saves to the database via the existing settings API"
    - "Signup/onboarding mode selector mentions Both availability in Settings"
    - "Onboarding still only offers Bible and Positivity (no Both option)"
  artifacts:
    - path: "src/app/(app)/settings/page.tsx"
      provides: "Three-option mode selector with Both"
      contains: "both"
    - path: "src/components/onboarding/ModeSelector.tsx"
      provides: "Brief mention of Both mode in Settings"
      contains: "Settings"
  key_links:
    - from: "src/app/(app)/settings/page.tsx"
      to: "src/app/api/settings/route.ts"
      via: "PUT /api/settings with mode: 'both'"
      pattern: "saveSettings.*mode.*both"
---

<objective>
Add "Both" as a third mode option in the Settings page and add a brief mention of Both mode availability on the onboarding ModeSelector.

Purpose: Users need a way to opt into Both mode (Settings only, not signup). The onboarding hint helps discoverability without pushing it during first-time setup.
Output: Updated settings page with 3-option mode selector, updated onboarding with brief mention.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-CONTEXT.md
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-RESEARCH.md
@src/app/(app)/settings/page.tsx
@src/components/onboarding/ModeSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Both mode option to Settings page</name>
  <files>src/app/(app)/settings/page.tsx</files>
  <action>
    Update the settings page to support the "Both" mode option:

    1. **MODE_CONFIG** (around line 113): Add 'both' entry:
       ```
       both: { label: 'Both', description: 'Bible and Positivity content', icon: Layers },
       ```
       The `Layers` icon is already imported (used for verse display mode section around line 427).

    2. **showModeConfirm state type** (line 136): Change from `'bible' | 'positivity' | null` to `'bible' | 'positivity' | 'both' | null`.

    3. **handleModeSwitch function** (line 237): Update the type parameter from `(newMode: 'bible' | 'positivity')` to `(newMode: 'bible' | 'positivity' | 'both')`.
       - Key change: When switching TO 'both', skip the confirmation dialog -- it's additive and non-destructive. The user doesn't lose access to anything.
       - Logic: `if (newMode === 'both') { saveSettings({ mode: 'both' }); return; }`
       - For switching FROM 'both' to 'bible' or 'positivity', show confirmation as normal.

    4. **Verse Display Mode section** (around line 422): Currently shows only when `settings?.mode === 'bible'`. Update to also show when Both mode is active AND effective view is Bible:
       - Change condition to: `settings?.mode === 'bible' || settings?.mode === 'both'`
       - Reasoning: Both users in Bible view should still see the verse mode toggle. Since the settings page always shows the full settings (not view-dependent), showing it for both/bible is appropriate.

    5. **Mode selector button rendering** (around line 371): The MODES array from constants now includes 'both', so the `.map()` will naturally render 3 buttons. Verify MODE_CONFIG has entries for all three.

    6. **Confirmation dialog text** (line 400): The existing text "Switch to {MODE_CONFIG[showModeConfirm].label}? This changes your daily content." works for all modes. No change needed.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Settings page renders 3 mode buttons (Bible, Positivity, Both).
    Selecting Both saves immediately without confirmation dialog.
  </verify>
  <done>
    Settings page shows Bible, Positivity, and Both as mode options. Both saves without confirmation. Verse Display Mode section visible for bible and both mode users.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Both mode mention to onboarding ModeSelector</name>
  <files>src/components/onboarding/ModeSelector.tsx</files>
  <action>
    Add a small text note below the mode options (before the Continue button) mentioning that Both mode is available in Settings.

    Between the `</div>` closing the mode options flex container (after line 135) and the `<Button>` (line 138), add:

    ```tsx
    <p className="text-xs text-center text-text-muted dark:text-text-muted-dark">
      Want both? You can access Bible and Positivity content together in Settings.
    </p>
    ```

    Do NOT add "Both" as a selectable option in the onboarding flow -- per CONTEXT.md, it's only available in Settings.
    Do NOT change the Mode type or MODE_OPTIONS array -- onboarding stays Bible/Positivity only.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    ModeSelector renders the hint text.
    No 'both' option in MODE_OPTIONS array.
  </verify>
  <done>
    Onboarding ModeSelector shows brief mention that Both mode is available in Settings. Onboarding flow unchanged -- still only Bible and Positivity selectable.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Settings page renders 3 mode options
3. Both mode saves without confirmation
4. Onboarding mentions Both availability
5. Onboarding does NOT offer Both as a selection
</verification>

<success_criteria>
- Settings page shows Bible, Positivity, Both as mode options
- Selecting Both immediately saves (no confirmation dialog)
- Selecting Bible or Positivity from Both shows confirmation
- Onboarding mode selector includes brief text mention of Both
- Onboarding only allows selecting Bible or Positivity
</success_criteria>

<output>
After completion, create `.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-03-SUMMARY.md`
</output>
