---
phase: 17-both-mode-url-driven-daily-content-without-mode-switching
plan: 06
type: execute
wave: 4
depends_on: ["17-02", "17-03", "17-04", "17-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Full Both mode flow works end-to-end: enable in Settings, see toggle, switch modes, content changes"
    - "Bible-only users see zero changes"
    - "Positivity-only users see zero changes"
    - "Build compiles without errors"
  artifacts: []
  key_links: []
---

<objective>
Build verification and human verification checkpoint for the complete Both mode feature.

Purpose: Verify the entire feature works end-to-end before shipping. Catch any visual or interaction issues that automated checks cannot detect.
Output: Verified working Both mode feature.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files></files>
  <action>
    Run full build verification:
    1. `npx tsc --noEmit` -- TypeScript compilation check
    2. `npm run build` -- Next.js production build

    If any errors, fix them. Common issues to watch for:
    - Type mismatches where 'both' is passed to something expecting only 'bible' | 'positivity'
    - Missing imports for useViewMode in updated components
    - ViewModeContext not available in GuestDailyWrapper rendering path (should be handled by default context value)
  </action>
  <verify>
    `npm run build` exits with code 0.
  </verify>
  <done>
    Production build passes. No TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Both mode feature:
    1. Settings page with three mode options (Bible, Positivity, Both)
    2. Pill toggle on daily content first slide for Both users
    3. Full UX switching (Prayer Wall, nav icons, daily content)
    4. localStorage persistence of active view mode
    5. URL-based initial mode (/positivity vs /)
  </what-built>
  <how-to-verify>
    Prerequisites: Log in as a test user, ensure dev server is running (`npm run dev`).

    **Test 1: Enable Both mode**
    1. Go to Settings
    2. Verify three mode options visible: Bible, Positivity, Both
    3. Tap "Both" -- should save immediately (no confirmation dialog)
    4. Navigate back to daily feed

    **Test 2: Pill toggle visibility and function**
    5. On the daily feed, verify a pill toggle (Bible | Positivity) appears at top of the first slide
    6. "Bible" should be highlighted (white bg) by default
    7. Tap "Positivity" -- content should reload with positivity content
    8. Verify scroll resets to top (today's card)
    9. Swipe to next day -- verify toggle appears on that day's first slide too

    **Test 3: Full UX switching**
    10. In Positivity view: verify Prayer Wall tab is HIDDEN in bottom nav
    11. Switch back to Bible via pill toggle
    12. Verify Prayer Wall tab REAPPEARS in bottom nav
    13. Verify nav icon images change between Bible and Positivity styles

    **Test 4: Persistence**
    14. Set to Positivity via pill toggle
    15. Refresh the page (Cmd+R / F5)
    16. Verify it stays on Positivity content (localStorage persistence)

    **Test 5: URL behavior**
    17. Navigate to /positivity URL directly -- should show Positivity content
    18. Navigate to / URL directly -- should show Bible content
    19. Verify URL does NOT change when using pill toggle

    **Test 6: Single-mode user unaffected**
    20. Switch back to "Bible" in Settings (or use a Bible-only test user)
    21. Verify NO pill toggle appears on daily feed
    22. Verify everything works exactly as before

    **Test 7: Onboarding mention**
    23. Visit /onboarding/mode (if accessible) -- verify "Want both?" text appears below mode options
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Human verifies full Both mode flow
3. Human confirms single-mode users are unaffected
</verification>

<success_criteria>
- Production build passes
- Both mode feature works end-to-end as verified by human
- No regressions for Bible-only or Positivity-only users
</success_criteria>

<output>
After completion, create `.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-06-SUMMARY.md`
</output>
