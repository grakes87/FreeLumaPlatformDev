---
phase: 17-both-mode-url-driven-daily-content-without-mode-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/migrations/125-add-both-to-user-mode-enum.cjs
  - src/lib/db/models/User.ts
  - src/context/AuthContext.tsx
  - src/lib/utils/constants.ts
  - src/app/api/settings/route.ts
autonomous: true

must_haves:
  truths:
    - "Database accepts 'both' as a valid value for users.mode column"
    - "TypeScript compiler accepts 'both' as a valid mode value across the app"
    - "Settings API accepts 'both' when updating user mode"
  artifacts:
    - path: "src/lib/db/migrations/125-add-both-to-user-mode-enum.cjs"
      provides: "ALTER TABLE to add 'both' to users.mode ENUM"
      contains: "ENUM('bible', 'positivity', 'both')"
    - path: "src/lib/db/models/User.ts"
      provides: "Updated mode type union"
      contains: "'both'"
    - path: "src/context/AuthContext.tsx"
      provides: "Updated UserData.mode type"
      contains: "'both'"
    - path: "src/lib/utils/constants.ts"
      provides: "Updated MODES array and Mode type"
      contains: "'both'"
    - path: "src/app/api/settings/route.ts"
      provides: "Updated Zod validation for mode"
      contains: "'both'"
  key_links:
    - from: "src/lib/db/migrations/125-add-both-to-user-mode-enum.cjs"
      to: "users.mode column"
      via: "ALTER TABLE MODIFY COLUMN"
      pattern: "ENUM.*both"
    - from: "src/lib/utils/constants.ts"
      to: "all mode consumers"
      via: "MODES array export"
      pattern: "MODES.*both"
---

<objective>
Add 'both' to the User.mode ENUM at the database and TypeScript type level, and update the Settings API to accept 'both' as a valid mode value.

Purpose: This is the foundational change that all other plans depend on. Without this, no component can reference 'both' as a valid mode, and the database will reject it.
Output: Migration file, updated User model, AuthContext type, constants, and settings API validation.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-CONTEXT.md
@.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-RESEARCH.md
@src/lib/db/models/User.ts
@src/context/AuthContext.tsx
@src/lib/utils/constants.ts
@src/app/api/settings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and model type update</name>
  <files>
    src/lib/db/migrations/125-add-both-to-user-mode-enum.cjs
    src/lib/db/models/User.ts
  </files>
  <action>
    Create migration `125-add-both-to-user-mode-enum.cjs` using raw SQL (NOT Sequelize changeColumn -- it's unreliable with MySQL ENUMs, per established pattern in migration 118):

    up: `ALTER TABLE users MODIFY COLUMN mode ENUM('bible', 'positivity', 'both') NOT NULL DEFAULT 'bible'`
    down: First `UPDATE users SET mode = 'bible' WHERE mode = 'both'`, then `ALTER TABLE users MODIFY COLUMN mode ENUM('bible', 'positivity') NOT NULL DEFAULT 'bible'`

    Use .cjs format with `module.exports` (ESM project convention for Sequelize CLI).

    Update `src/lib/db/models/User.ts`:
    - Line 23: Change `mode: 'bible' | 'positivity'` to `mode: 'bible' | 'positivity' | 'both'`
    - Line 111: Change `declare mode: 'bible' | 'positivity'` to `declare mode: 'bible' | 'positivity' | 'both'`
  </action>
  <verify>
    Run `npx sequelize-cli db:migrate` -- migration 125 applies without error.
    Verify: `mysql -u root -e "SHOW COLUMNS FROM users LIKE 'mode'" freeluma_dev` shows ENUM('bible','positivity','both').
  </verify>
  <done>
    users.mode column accepts 'both' values. User.ts type includes 'both'.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TypeScript types and API validation</name>
  <files>
    src/context/AuthContext.tsx
    src/lib/utils/constants.ts
    src/app/api/settings/route.ts
  </files>
  <action>
    1. Update `src/context/AuthContext.tsx` line 22:
       Change `mode: 'bible' | 'positivity'` to `mode: 'bible' | 'positivity' | 'both'`

    2. Update `src/lib/utils/constants.ts`:
       - Line 4: Change `export const MODES = ['bible', 'positivity'] as const;` to `export const MODES = ['bible', 'positivity', 'both'] as const;`
       - Line 5 (`export type Mode = typeof MODES[number]`) remains unchanged -- it auto-derives from MODES array.

    3. Update `src/app/api/settings/route.ts` line 15:
       Change `mode: z.enum(['bible', 'positivity']).optional()` to `mode: z.enum(['bible', 'positivity', 'both']).optional()`

    4. Also in `src/app/api/settings/route.ts`, find the UserSettings response type (around line 154) that has `mode: 'bible' | 'positivity'` and update to `mode: 'bible' | 'positivity' | 'both'`.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- no type errors related to mode.
    Grep: `grep -rn "'bible' | 'positivity'" src/` returns no matches (all updated to include 'both').
  </verify>
  <done>
    All TypeScript type definitions include 'both'. Settings API Zod schema validates 'both'. No type errors.
  </done>
</task>

</tasks>

<verification>
1. `npx sequelize-cli db:migrate` succeeds
2. `npx tsc --noEmit` passes with no mode-related errors
3. Settings API accepts `mode: 'both'` in PUT request
4. MODES constant includes 'both' and Mode type derives it
</verification>

<success_criteria>
- Migration 125 creates 'both' enum value in users.mode column
- All TypeScript mode types include 'both' (User.ts, AuthContext.tsx, constants.ts, settings route)
- Settings API Zod validation accepts 'both'
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-both-mode-url-driven-daily-content-without-mode-switching/17-01-SUMMARY.md`
</output>
