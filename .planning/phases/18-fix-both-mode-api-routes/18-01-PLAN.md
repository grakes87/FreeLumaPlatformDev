---
phase: 18-fix-both-mode-api-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/daily-posts/route.ts
  - src/app/api/daily-posts/[date]/route.ts
  - src/hooks/useDailyContent.ts
autonomous: true
requirements: [DAILY-01, DAILY-05]

must_haves:
  truths:
    - "Both-mode user visiting / sees today's daily content (not a 404)"
    - "Both-mode user navigating to /daily/[date] sees content for that date (not a 404)"
    - "Both-mode user in Positivity view navigating to /daily/[date] sees Positivity content (not Bible)"
    - "Guest users and single-mode users are unaffected by these changes"
  artifacts:
    - path: "src/app/api/daily-posts/route.ts"
      provides: "Today's daily content with Both-mode resolution + optional mode query param"
      contains: "resolveContentMode"
    - path: "src/app/api/daily-posts/[date]/route.ts"
      provides: "Date-specific daily content with Both-mode resolution + optional mode query param"
      contains: "resolveContentMode"
    - path: "src/hooks/useDailyContent.ts"
      provides: "Client hook that passes effectiveMode to daily-posts API for Both users"
      contains: "mode="
  key_links:
    - from: "src/hooks/useDailyContent.ts"
      to: "/api/daily-posts"
      via: "mode query parameter in fetch URL"
      pattern: "mode="
    - from: "src/app/api/daily-posts/route.ts"
      to: "resolveContentMode"
      via: "import from constants"
      pattern: "resolveContentMode"
    - from: "src/app/api/daily-posts/[date]/route.ts"
      to: "resolveContentMode"
      via: "import from constants"
      pattern: "resolveContentMode"
---

<objective>
Fix Both-mode daily content API routes so Both-mode users see content instead of 404 errors, and wire the frontend hook to pass the user's active view mode to the API.

Purpose: Both-mode users (mode='both') currently get 404s on daily content pages because the API queries `WHERE mode='both'` which matches no daily_content rows. This is a live production bug.

Output: Three files fixed -- two API routes use `resolveContentMode()` with optional `?mode=` query param override, and the `useDailyContent` hook passes `effectiveMode` from ViewModeContext.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-fix-both-mode-api-routes/18-RESEARCH.md
@src/app/api/daily-posts/route.ts
@src/app/api/daily-posts/[date]/route.ts
@src/hooks/useDailyContent.ts
@src/context/ViewModeContext.tsx
@src/lib/utils/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix daily-posts API routes with resolveContentMode + mode query param</name>
  <files>src/app/api/daily-posts/route.ts, src/app/api/daily-posts/[date]/route.ts</files>
  <action>
Fix both daily-posts API routes so Both-mode users get content instead of 404.

**For `/api/daily-posts/route.ts` (line 27):**
1. Add import: `import { resolveContentMode } from '@/lib/utils/constants';` (add to existing LANGUAGES import line or separate)
2. After the existing `const url = new URL(req.url);` block (line 42-46), read a `mode` query param:
   ```
   const modeParam = url.searchParams.get('mode');
   ```
3. Change the mode assignment inside `if (user)` block (line 27) from:
   ```
   mode = user.mode;
   ```
   to:
   ```
   if (modeParam === 'bible' || modeParam === 'positivity') {
     mode = modeParam;
   } else {
     mode = resolveContentMode(user.mode);
   }
   ```
   Note: The mode query param reading happens after `url` is defined (line 42), so move the user-block mode assignment AFTER the url parsing, or read the param inside the user block using `new URL(req.url)`. The cleanest approach: parse `url` and `modeParam` BEFORE the user block (move the url parsing up), then use `modeParam` in the user block.

**For `/api/daily-posts/[date]/route.ts` (line 38):**
1. Add import: `import { resolveContentMode } from '@/lib/utils/constants';` (add to existing LANGUAGES import or separate)
2. The `url` is already parsed at line 53 for timezone. Read mode param there:
   ```
   const modeParam = url.searchParams.get('mode');
   ```
3. Change line 38 from:
   ```
   mode = user.mode;
   ```
   to:
   ```
   if (modeParam === 'bible' || modeParam === 'positivity') {
     mode = modeParam;
   } else {
     mode = resolveContentMode(user.mode);
   }
   ```
   Note: `url` is defined at line 53 which is AFTER the user block (line 32-42). Either move the url parsing above the user block, or parse the URL inside the user block. The cleanest approach: move `const url = new URL(req.url);` and `const modeParam = url.searchParams.get('mode');` to just before the user block (after timezone/language defaults are set at line 27-29), then the rest of the url usages (timezoneParam at line 54-56) still work fine.

**Important:** Do NOT change behavior for guest users or single-mode users. Guests still default to 'bible'. Single-mode users (bible or positivity) get `resolveContentMode()` which returns their exact mode. Only Both-mode users are affected: they get the frontend's active view mode via `?mode=` param, or fall back to 'bible' (resolveContentMode default).
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Manually verify the import and logic by reading both files.
  </verify>
  <done>
Both `/api/daily-posts` and `/api/daily-posts/[date]` import and use `resolveContentMode()`, accept optional `?mode=` query param, and never pass 'both' to the database WHERE clause.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire useDailyContent hook to pass effectiveMode from ViewModeContext</name>
  <files>src/hooks/useDailyContent.ts</files>
  <action>
Update the `useDailyContent` hook to accept an optional `mode` parameter and pass it as a query parameter to the daily-posts API.

1. Update the function signature to accept a mode parameter:
   ```typescript
   export function useDailyContent(date?: string, mode?: string): UseDailyContentReturn {
   ```

2. Inside the `fetchContent` callback (line 74), update the URL construction (lines 80-82) to include the mode param when provided:
   ```typescript
   const modeParam = mode ? `&mode=${encodeURIComponent(mode)}` : '';
   const url = date
     ? `/api/daily-posts/${date}?timezone=${tz}${modeParam}`
     : `/api/daily-posts?timezone=${tz}${modeParam}`;
   ```

3. Add `mode` to the dependency array of the `fetchContent` useCallback (line 146):
   ```typescript
   }, [date, mode]);
   ```

4. Now update the call site in `DailyPostCarousel.tsx`. In the `SingleDayCarousel` component (around line 53), it needs to get the effectiveMode from ViewModeContext and pass it to `useDailyContent`:
   - Add import: `import { useViewMode } from '@/context/ViewModeContext';`
   - Inside `SingleDayCarousel`, add: `const { effectiveMode } = useViewMode();`
   - Change the hook call from `useDailyContent(date)` to `useDailyContent(date, effectiveMode)`

**Important:** The `mode` parameter is optional so existing callers (preview-feed page) are unaffected. Only `SingleDayCarousel` needs to pass it since that's the component used for `/daily/[date]` navigation.

**Files touched by this task:** `src/hooks/useDailyContent.ts` AND `src/components/daily/DailyPostCarousel.tsx` (minor 3-line change to pass effectiveMode).
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Read the updated `useDailyContent.ts` and `DailyPostCarousel.tsx` to verify the mode wiring.
  </verify>
  <done>
The `useDailyContent` hook accepts and passes an optional `mode` param to the API. `SingleDayCarousel` passes `effectiveMode` from ViewModeContext, so Both-mode users viewing Positivity content see Positivity daily content on `/daily/[date]` pages.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Both API routes import and call `resolveContentMode()`
3. Both API routes accept `?mode=` query parameter with validation (only 'bible' or 'positivity' accepted)
4. `useDailyContent` hook passes mode param in fetch URL when provided
5. `SingleDayCarousel` reads effectiveMode from ViewModeContext and passes to hook
6. No changes affect guest users or single-mode users
</verification>

<success_criteria>
- Both-mode user hitting `/api/daily-posts` gets content (mode resolved to 'bible' by default or overridden by `?mode=` param)
- Both-mode user hitting `/api/daily-posts/2026-03-12` gets content (not 404)
- Both-mode user in Positivity view navigating to `/daily/[date]` gets Positivity content
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-both-mode-api-routes/18-01-SUMMARY.md`
</output>
